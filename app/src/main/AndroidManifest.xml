<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for camera and storage -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Internet permission for API calls -->

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OutfitGenerator">
        
        <!-- Splash Activity - Main Entry Point -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        
        <!-- Registration Activity -->
        <activity
            android:name=".RegistrationActivity"
            android:exported="false" />
        
        <!-- Forgot Password Activity -->
        <activity
            android:name=".ForgotPasswordActivity"
            android:exported="false" />
        
        <!-- Reset Password Activity -->
        <activity
            android:name=".ResetPasswordActivity"
            android:exported="false" />
        
        <!-- Home Activity - Landing Screen -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />
        
        <!-- Upload Clothes Activity -->
        <activity
            android:name=".UploadClothesActivity"
            android:exported="false" />
        
        <!-- Wardrobe Activity -->
        <activity
            android:name=".WardrobeActivity"
            android:exported="false" />
        
        <!-- Suggestions Activity -->
        <activity
            android:name=".SuggestionsActivity"
            android:exported="false" />
        
        <!-- AI Outfit Suggestion Activity -->
        <activity
            android:name=".OutfitSuggestionActivity"
            android:exported="false" />
        
        <!-- AI Outfit Suggestion Result Activity -->
        <activity
            android:name=".OutfitSuggestionResultActivity"
            android:exported="false" />
        
        <!-- Main Activity - Original -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        
        <!-- File Provider for Camera -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>
    </application>

</manifest>